import{describe,expect,it}from"vitest";import{canTransition}from"./state-machine";describe("state transitions",()=>{it("allows happy path",()=>{expect(canTransition("idle","recording")).toBe(true);expect(canTransition("recording","processing")).toBe(true);expect(canTransition("processing","transcribing")).toBe(true);expect(canTransition("transcribing","inserting")).toBe(true);expect(canTransition("inserting","idle")).toBe(true)});it("rejects invalid jumps",()=>{expect(canTransition("idle","transcribing")).toBe(false)})})